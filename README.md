Symfony UX Sync plugin
======================

About
-----

Provides the "`package.json` sync" of [Symfony UX][1] for projects that do not use Symfony Flex.

Require this package inside your `require-dev` section of the `composer.json`.

[1]: https://symfony.com/ux
